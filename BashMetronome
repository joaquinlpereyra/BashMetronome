#!/usr/bin/env bash
#A small metronome written for the command line

function Play {
	# main function
	tempo=$(bc -l <<< 60/"$1")
	echo "Your metonomer is set and ready at "$1" BPM. Press Ctrl+C to quit"
	while [[ : ]]; do
		FirstSound 
		SecondSound
	done
}

function FirstSound {
	aplay -q ./src/first.wav & echo -ne ' + ' & sleep "$tempo"
}

function SecondSound {
	aplay -q ./src/second.wav & echo -ne ' - ' &sleep "$tempo"
}

Play $1
